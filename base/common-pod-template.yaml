spec:
  template:
    metadata:
      annotations:
        rollout-restart-ts: "abc123"
    spec:
      containers:
        - name: nginx
          image: nginx:alpine
          volumeMounts:
            - name: config-volume
              mountPath: /usr/share/nginx/html
              readOnly: true
